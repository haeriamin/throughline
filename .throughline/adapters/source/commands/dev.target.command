ns = dev
cmd = target
persona = orchestrator
description = Register, inspect, or update an external target project
argument_hint = register <path> [--id <id>] [--new] | inspect <id> | update <id> <k>=<v> | list
